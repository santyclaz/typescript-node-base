services:
  service:
    build:
      context: .
      target: dev
    volumes:
      - .:/workspace:cached
    ports:
      - "${PORT:-3000}:${PORT:-3000}" # webserver port
    environment:
      TZ: "${TZ:-America/Los_Angeles}"
      PORT: "${PORT:-3000}"
    command: sleep infinity

services:
  service:
    build:
      context: .
      target: dev
    volumes:
      - .:/workspace:cached
    ports:
      - "${PORT:-3000}:${PORT:-3000}" # webserver port
    command: sleep infinity
